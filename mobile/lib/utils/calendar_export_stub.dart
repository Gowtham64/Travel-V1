/// Non-web stub. On mobile/desktop we share the .ics via the OS instead of a
/// browser download, so this is never called; it exists to satisfy the
/// conditional import.
void downloadIcs(String filename, String content) {}
