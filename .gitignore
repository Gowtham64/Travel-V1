# Backend
backend/node_modules/
backend/.env

# Flutter
mobile/.dart_tool/
mobile/build/
mobile/.flutter-plugins
mobile/.flutter-plugins-dependencies
mobile/ios/Pods/
mobile/android/.gradle/

# Local Cloudflare Pages build staging directory
gh-pages-deploy/

# OS / editor junk
.DS_Store
**/.DS_Store
.agents/

# Build outputs & large binaries (distributed via GitHub Releases, not committed)
*.apk
*.ipa
*.aab
*.zip
*.app/
Voyplan.app/
web/Voyplan.ipa
live.js

# Local MCP config (contains Hostinger API token — never commit)
.mcp.json

# Autonomous AI Engineering System (tracked in repo for Render cloud deployment)
logs/
*.log
agents-office/
